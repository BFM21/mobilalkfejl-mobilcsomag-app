<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobilcsomag"
        tools:targetApi="31">
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.DashboardActivity"></activity>
        <activity android:name=".views.CustomPackageActivity"></activity>
        <activity android:name=".views.ProfileActivity"></activity>
        <activity android:name=".views.LoginActivity"></activity>
        <activity android:name=".views.RegisterActivity"></activity>
        <activity android:name=".views.EditPersonalDataActivity"></activity>
        <activity android:name=".views.PackageActivity"></activity>
        <activity android:name=".views.SplashScreenActivity"></activity>
        <activity android:name=".views.AddedPackagesActivity"></activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>